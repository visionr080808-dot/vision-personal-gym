# VISION Personal Gym サイト更新手順書（初心者向け）

最終更新: 2026-07-24

パソコンに何もインストールしなくても、**ブラウザだけ**で「料金」「トップの文言」「写真」を
変更できます。GitHubという「コードの保管庫（サイトの原稿置き場）」を直接編集する方法です。

---

## 0. 最初に知っておくこと

- サイトの文章・料金は、ぜんぶ **1つのファイル** にまとまっています。
  ファイル名: `src/data/site.ts`
  ここだけ見れば、店名・住所・料金・メニュー・お客様の声などが全部見つかります。
- 編集して保存（＝コミット）しても、**それだけでは本番サイトにまだ反映されません**。
  最後に「デプロイ（公開作業）」という一手間が必要です（→ 手順の最後で説明します）。
- 見た目のデザイン（色や配置）を変えるファイルは別にありますが、
  この手順書では **文章と数字の変更だけ** を扱います。デザインを変えたい時はClaudeに直接頼んでください。

---

## 1. GitHubにログインする

1. ブラウザで https://github.com を開く
2. 右上「Sign in」から、`vision.r080808@gmail.com` 用のGitHubアカウントでログイン

---

## 2. 編集したいファイルを開く

1. ログイン後、このURLを開く：
   **https://github.com/visionr080808-dot/vision-personal-gym**
2. 画面の中にあるフォルダ一覧から `src` → `data` の順にクリックして進む
3. `site.ts` というファイルをクリックして開く

---

## 3. ファイルを編集する

1. ファイル画面の右上にある **鉛筆マーク（✏️ Edit this file）** をクリック
2. 画面が「編集できるテキストエディタ」に変わります
3. 変更したい部分だけを書き換える（下の「よくある変更例」を参照）
4. 変更が終わったら、画面をそのまま下にスクロール

### よくある変更例

#### ① 料金を変える
`site.ts` の中に、こういう並びの部分があります：

```
{ name: "4回", per: "6,250", total: "25,000" },
```

- `per` = 1回あたりの金額
- `total` = 合計金額（または月額）

数字の部分だけを書き換えてください。**カンマ（,）や引用符（" "）は消さないように注意**。

#### ② トップの見出し文言を変える
`site.ts` の中の `tagline:` という行を探します：

```
tagline: "あなたが思い描く理想の自分を実現する場所",
```

`" "`（ダブルクォーテーション）の中の文章だけを書き換えてください。
（実際の見出しの改行位置は別ファイルで管理していますが、まず文章だけ変えたい場合はここでOKです。
改行位置も変えたい場合はClaudeに一言お願いしてください）

#### ③ 住所・電話番号・LINE・Instagramのリンクを変える
同じく `site.ts` の中に、それぞれの行があります：

```
area: "岡山県苫田郡鏡野町上森原353-3",
tel: "080-2889-8819",
line: "https://lin.ee/VB6XkDR",
instagram: "https://www.instagram.com/yu_fitness_jp",
```

`" "` の中身だけを書き換えてください。

#### ④ お客様の声・FAQを変える／増やす
少し下にスクロールすると `voices`（お客様の声）や `faqs`（よくある質問）という
まとまりがあります。同じ形をコピーして増やすこともできますが、
慣れないうちは既存の文章の書き換えだけにとどめるのが安全です。

#### ⑤ 写真を差し替える
写真ファイルは `public/images/` フォルダの中にあります。
GitHubの画面で `public` → `images` と進み、差し替えたい画像と
**同じファイル名**でアップロードし直すと、コード側は変更不要です。
（ファイル名を変えたい場合は `site.ts` 側の記述もあわせて直す必要があるので、
その場合はClaudeにお願いするのが確実です）

---

## 4. 変更を保存する（コミット）

編集画面の下の方にスクロールすると、緑色のボタンがあります：

1. 「Commit changes...」というボタンをクリック
2. 上の欄（コミットメッセージ）に、何を変えたか簡単に書く
   例：「料金を変更」「トップの文言を修正」など（日本語でOK）
3. 下の「Commit directly to the `main` branch.」が選ばれていることを確認
4. 緑色の「Commit changes」ボタンをクリック

これで変更の保存は完了です。

---

## 5. サイトに反映させる（デプロイ）

**ここが唯一の注意点です。** 現状、GitHubを更新しただけでは本番サイトに自動反映されません
（自動連携が未設定のため）。反映には以下のどちらかが必要です：

- **方法A（かんたん）**: Claudeに「GitHubを更新したのでデプロイして」と伝える
  → Claudeが1分程度で本番反映します
- **方法B（自分で）**: パソコンのターミナルで、このフォルダに移動してから
  ```
  npm run build
  npx wrangler pages deploy out --project-name=vision-personal-gym --branch=main
  ```
  を実行する（詳しくは `HANDOFF.md` を参照）

将来的にGitHubとCloudflareの自動連携が設定できれば、方法Aすら不要になり
「GitHubで保存するだけで自動的にサイトに反映される」ようになります。
（この設定はCloudflareダッシュボードでの承認作業が必要で、`HANDOFF.md` の
「10. 未対応・申し送り事項」に手順を記載しています）

---

## 6. こまったとき

- 編集を間違えて元に戻したい → 保存前ならページを再読み込みすればOK。
  保存後に戻したい場合は、GitHubの「Commits」履歴から前のバージョンを見て、
  Claudeに「〇〇の変更を元に戻して」と頼んでください
- どこを直せばいいかわからない → Claudeに「料金を◯◯に変えたい」と
  日本語でそのまま伝えれば、ファイルの場所ごと代わりに直してくれます
- サイトが真っ白になった・崩れた → 何も自分で直そうとせず、すぐClaudeに
  「サイトがおかしくなった」と伝えてください

---

## 参考: サイトの技術的な詳細

もっと詳しい構成（デザイン・ページ構成・アカウント情報など）は、
同じフォルダにある **`HANDOFF.md`** にまとまっています。
